from setuptools import setup

setup(name='scos_distributions_beta',
      version='0.1',
      description='Gaussian distributions',
      packages=['scos_distributions_beta'],
      author = 'scoscot',
      zip_safe=False)
