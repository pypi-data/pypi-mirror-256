from setuptools import setup

setup(name='dgood_gabinodist',
      version='0.3',
      description='Gaussian and binomial distributions',
      long_description='The dgood-gabinodist package has classes that will do the following for Gaussian and Binomial distributions: Read data from file, calculate the mean and standard deviation, find the probability distribution function, display plots, add two distributions. This was created as part of the Udacity lesson: introduction to object oriented programming with Python.',
      packages=['dgood_gabinodist'],
      zip_safe=False)
