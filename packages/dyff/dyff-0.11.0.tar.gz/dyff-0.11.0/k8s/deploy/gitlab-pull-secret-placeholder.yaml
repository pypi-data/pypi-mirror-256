apiVersion: v1
kind: Secret
metadata:
  name: gitlab-pull
  labels:
    app: audit
    component: gitlab
# This is stringData so that we can use the urlsafe base64 alphabet everywhere
stringData:
  ALIGNMENTLABS_GITLAB_AUDIT_READER_ACCESS_TOKEN: ""
