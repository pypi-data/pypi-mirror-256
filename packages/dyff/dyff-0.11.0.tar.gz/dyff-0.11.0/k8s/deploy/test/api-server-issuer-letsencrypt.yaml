apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  name: api-server-letsencrypt-staging
  labels:
    app: api-server
    component: issuer
spec:
  acme:
    server: https://acme-staging-v02.api.letsencrypt.org/directory
    email: certs@alignmentlabs.ai
    privateKeySecretRef:
      name: api-server-letsencrypt-staging
    solvers:
      - http01:
          ingress:
            name: api-server-ingress
