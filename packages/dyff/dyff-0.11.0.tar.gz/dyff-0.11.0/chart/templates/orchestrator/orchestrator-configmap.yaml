apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "dyff.orchestrator.fullname" . }}
  labels:
    {{- include "dyff.orchestrator.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.orchestrator.extraEnvVarsConfigMap }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
