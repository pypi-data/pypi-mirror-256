apiVersion: v1
kind: Secret
metadata:
  name: {{ include "dyff.api.fullname" . }}-key-signing
  labels:
    {{- include "dyff.api.labels" . | nindent 4 }}
# This is stringData so that we can use the urlsafe base64 alphabet everywhere
stringData:
  ALIGNMENTLABS_API_KEY_SIGNING_SECRET: ""
