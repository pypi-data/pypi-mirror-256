apiVersion: v1
kind: Secret
metadata:
  name: {{ include "dyff.api.fullname" . }}
  labels:
    {{- include "dyff.api.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.api.extraEnvVarsSecret }}
  {{ $key }}: {{ $value | b64enc }}
  {{- end }}
