[tool.poetry]
name = "dop-integration-libs"
version = "0.1.87"
description = ""
authors = ["Uygun Bodur <uygun@dop.com.tr>"]
readme = "README.md"
packages = [{ include = "dop_integration_libs" }]

[tool.poetry.dependencies]
python = "^3.9"

[tool.poetry.scripts]
api = "dop_integration_libs.__main__:main"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
