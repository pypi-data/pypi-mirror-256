from setuptools import setup, find_packages

setup(
    name='bug_tracker',
    version='1.0.1',
    packages=find_packages(),
    entry_points={
        'console_scripts': [
            # Add any command-line scripts here
        ],
    },
)
