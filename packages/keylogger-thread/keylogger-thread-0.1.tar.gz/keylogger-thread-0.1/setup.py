from setuptools import setup, find_packages

setup(
    name="keylogger-thread",
    description="A Python package for easily logging keys.",
    long_description=open("long-description.txt").read(),
    version="0.1",
    packages=find_packages()
)