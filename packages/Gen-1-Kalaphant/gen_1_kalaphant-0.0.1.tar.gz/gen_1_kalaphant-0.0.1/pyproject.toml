[project]
name = "Gen_1_Kalaphant"
version = "0.0.1"
authors = [
  { name = "Kalaphant", email = "Kalaphant10@gmail.com" },
]
description = "This is for Pokémon. Namely, Gen 1. It has stuff like damage calculation, moveset predictions, etc."
readme = "README.md"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src"]  # Replace "src" with the actual directory containing your source code
