from setuptools import setup

setup(name='glenn_distributions',
      version='0.1',
      description='Gaussian & Binomial distributions',
      packages=['glenn_distributions'],
      author='Glenn Teh',
      zip_safe=False)
