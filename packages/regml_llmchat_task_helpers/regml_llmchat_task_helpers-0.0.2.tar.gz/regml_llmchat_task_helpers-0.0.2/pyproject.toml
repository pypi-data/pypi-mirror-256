[tool.poetry]
name = "regml_llmchat_task_helpers"
version = "0.0.2"
description = "Regscale ML LLMChat Task Helpers"
authors = ["Luke Bechtel <lbechtel@regscale.com>"]
packages = [{ include = "regml_llmchat_task_helpers" }]

[tool.poetry.dependencies]
python = ">=3.8"
numpy = "^1.2.3"
regml_core = "^0.0.2" 
regml_task_registry_hardcoded = "^0.0.1"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
