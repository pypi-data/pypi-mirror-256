#import "../category/baseFragment.graphql"
#import "../inventory/baseFragment.graphql"

{
  categories(permission: "plank.create_item") {
    ...CategoryBaseParts
    inventory {
      ...InventoryBaseParts
    }
  }
}
