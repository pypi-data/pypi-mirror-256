mutation ($idOrBarcode: ID!) {
  checkIn(idOrBarcode: $idOrBarcode) {
    checkedOutItem {
      process {
        id
        checkInForm
      }
      item {
        id
        name
      }
    }
    status
  }
}
