[tool.poetry]
name = "abuseipdb2iptables"
version = "0.2.1"
description = ""
authors = ["Bruno Thomas <bruno@barreverte.fr>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.8"

[tool.poetry.scripts]
abuseipdb2iptables = "abuseipdb2iptables.cli:entry_point"

[tool.poetry.group.dev.dependencies]
pytest = "^7.2.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
