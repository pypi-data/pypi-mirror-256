[tool.poetry]
name = "poetry-test-project-2"
version = "0.1.1"
description = "Just a dummy project to test packaging. This package will be removed."
authors = ["Joost Berkhout <joost.berkhout@vu.nl>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
ipython = "^8.21.0"
pre-commit = "^3.6.1"
pytest = "^8.0.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
