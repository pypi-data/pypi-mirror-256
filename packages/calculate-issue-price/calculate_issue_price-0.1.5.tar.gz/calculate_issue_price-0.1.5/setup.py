from setuptools import setup
setup(
    name='calculate_issue_price',
    version='0.1.5',
    py_modules=['calculate_issue'],
    install_requires=[
        # List any dependencies here if needed
    ],
)