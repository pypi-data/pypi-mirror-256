from setuptools import setup, find_packages

setup(
    name='yuag',
    version='0.0.47',
    packages=find_packages(),
    install_requires=[
        # قائمة بالاعتماديات اللازمة لتشغيل المكتبة
    ]
)