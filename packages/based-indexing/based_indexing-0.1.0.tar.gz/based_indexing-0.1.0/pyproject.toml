[tool.poetry]
name = "based-indexing"
version = "0.1.0"
description = ""
authors = ["jmillana"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
num2words = "^0.5.13"
number-parser = "^0.3.2"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
